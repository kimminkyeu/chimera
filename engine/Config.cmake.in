@PACKAGE_INIT@
include("${CMAKE_CURRENT_LIST_DIR}/@TARGETS_EXPORT_NAME@.cmake")

# ------------------------------------------------------------------
# 이 안에 작성한 내용은 ***Config.cmake 파일에 삽입됩니다.

# find_package(...)를 통해 패키지 연결시, 자동으로 아래의 변수가 설정됩니다.
SET(  @CMAKE_PROJECT_NAME@_INCLUDE_DIR    @CHIMERA_EXPORT_INCLUDE_DIR@  )
SET(  @CMAKE_PROJECT_NAME@_LIBRARIES      @CHIMERA_EXPORT_LIBRARIES@    )
SET(  @CMAKE_PROJECT_NAME@_SHARED_LIB     @CM_SHARED_LIB@               ) 

# How to Import

